requests
pandas
flask
flask-cors
gunicorn
python-dateutil
beautifulsoup4

# NOTE: Heavy dev-only packages (matplotlib, seaborn, jupyter) removed for production build speed.
# If you need them on Render, add back explicitly or use app_requirements_dev.txt.